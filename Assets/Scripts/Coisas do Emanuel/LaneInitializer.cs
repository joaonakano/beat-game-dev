using UnityEngine;

public class LaneInitializer : MonoBehaviour
{
    void Start()
    {
         /*
        // Acha todas as lanes na cena
        Lane[] allLanes = FindObjectsOfType<Lane>();

        // Pega a tecla especial salva no KeybindManager
        KeyCode specialKey = KeybindManager.Instance.GetKey("Special");

        // Atribui a tecla especial para todas as lanes
        foreach (Lane lane in allLanes)
        {
            lane.specialInput = specialKey;
        }*/
    }
}
